name: shift_tracker
description: Череда — приложение для планирования и учёта рабочих смен.
publish_to: "none"
version: 1.0.2+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  path_provider: ^2.1.4
  flutter_secure_storage: ^9.2.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/chereda_app_icon.png
